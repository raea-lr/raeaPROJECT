{% extends 'base.html.twig' %}

{% block title %}Edit Client{% endblock %}

{% block body %}
    <div class="container">
        <h1>Edit Client</h1>

        {{ include('client/_form.html.twig', {'button_label': 'Update'}) }}

        {{ include('client/_delete_form.html.twig') }}

        <a href="{{ path('client_index') }}" class="btn btn-dark">Back to list</a>
    </div>
{% endblock %}
